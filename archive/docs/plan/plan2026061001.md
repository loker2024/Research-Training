# 下一步计划：步骤 6 可视化与深入分析

## Summary
当前第 5 步消融实验已完成：核心正式结果覆盖 `ETTh1/ETTm1 × 5 horizons × 5 models = 50` 组，消融结果覆盖 `16` 组。下一步不再训练新模型，先把当前成果整理成报告可直接使用的图表、结论和进度记录。

## Key Changes
- 新增 `scripts/visualize_results.py`：
  - 读取 `results/v1_csv/formal/formal_seed42_all.csv`、`results/v1_csv/ablation/ablation_seed42_vs_formal_comparison.csv`。
  - 输出到 `results/figures/`：核心模型 MSE/MAE/R² 趋势图、各 horizon 最优模型图、训练时间/参数量复杂度图、消融 `delta_MSE_pct` 对比图。
  - 加载已有 checkpoint 做推理，不重训模型；默认导出 `ETTh1 h96 patchtst`、`ETTh1 h336 patchtst`、`ETTm1 h96 autoformer`、`ETTm1 h336 autoformer` 的预测-真实曲线与残差图。
  - 预测曲线只画目标列，使用 `data/processed/{dataset}/scaler.npz` 还原到原始量纲。
- 新增 `docs/step/analysis_step6.md`：
  - 汇总核心结论：不同模型随预测步长的性能衰减、ETTh1/ETTm1 差异、PatchTST/Autoformer 优势、Transformer/LSTM/Informer 退化情况。
  - 汇总消融结论：Autoformer 分解模块、Auto-Correlation、PatchTST patching、Channel Independence 的贡献。
  - 引用生成的图表路径，作为后续实验报告素材。
- 更新 `README.md`：
  - 增加第 6 步可视化命令。
  - 说明图表输出目录和 checkpoint 推理不重训的原则。

## Test Plan
- 运行静态检查：
  - `python -m py_compile scripts/visualize_results.py`
- 运行可视化脚本：
  - `python scripts/visualize_results.py`
- 验收结果：
  - `results/figures/` 至少生成核心趋势图、复杂度图、消融图、预测曲线图、残差图。
  - `docs/step/analysis_step6.md` 中引用的图片路径全部存在。
  - checkpoint 推理样本输出 shape 与对应 horizon 一致。
  - `docs/progress.md` 记录完成时间、完成内容、修改文件、测试结果和下一步任务。

## Assumptions
- 当前目标是推进第 6 步分析，不修改模型结构、不重跑正式训练。
- ECL 仍暂不进入主结果分析，只在后续作为高维 smoke 或附录补充。
- 当前未提交/未跟踪的消融成果视为有效工作成果，后续实现时只追加第 6 步相关文件，不清理或回退已有改动。
