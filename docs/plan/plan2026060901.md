# 重新分析后的下一步计划

## Summary
当前仓库实际保留的正式结果只有 `ETTh1 h24/h48 × 5 模型`，均为 `formal_seed42`。PatchTST 在 h24、h48 都是当前最佳。下一步不应直接进入消融或报告，而应先完成 ETTh1 剩余 h96/h168/h336，并在提交当前稳定快照后再继续训练。

## Key Changes
- 先处理版本边界：
  - 提交已暂存的稳定内容：`.gitignore`、`README.md`、`docs/progress.md`、`results/` 中 ETTh1 h24/h48 正式结果。
  - 暂不把未暂存的 `scripts/*.py` 修改和已移除的 Mac CUDA 临时训练计划混进同一提交，先单独确认其用途。
- 继续正式实验：
  - 运行 ETTh1 h96/h168/h336，模型为 `lstm,transformer,informer,autoformer,patchtst`。
  - 使用现有配置参数：20 epochs、patience=5、batch_size=32、seed=42、run_tag=`formal_seed42`、sample_limit=0、skip_existing=true。
  - 命令可用现有配置续跑，但实际会跳过已存在 h24/h48：
    `python scripts/run_experiments.py --config configs/core_experiment_etth1_ettm1_formal.json`
- ETTh1 五步长完成后，再开始 ETTm1 五步长正式实验。
- ECL 暂不进入主正式矩阵；等 ETTh1/ETTm1 完成后，只做高维可行性 smoke 或单独附录实验。

## Test Plan
- 每批训练后检查：
  - 每个 `results/h{horizon}/ETTh1/{model}/formal_seed42/` 下同时存在 `_results.npy` 和 `_summary.json`。
  - 汇总结果只包含 `formal_seed42`，不混入 quick/smoke/optv2。
  - `docs/progress.md` 追加本批训练时间、指标、结果文件和下一步。
- ETTh1 完成后生成总表：
  - `formal_seed42_etth1_all_summary.csv`
  - `formal_seed42_etth1_all_summary.md`
- 汇总重点：
  - 每个 horizon 的最低 MSE 模型。
  - PatchTST 是否持续领先。
  - Transformer 在 h48 已明显退化，后续 h96/h168/h336 需重点观察。

## Assumptions
- 当前目标是推进研究项目，而不是修改模型代码。
- 正式报告主线以 ETTh1/ETTm1 为核心，ECL 作为高维补充。
- 当前已完成结果值得保留并提交；临时 quick/smoke/optv2 结果已清理，不再恢复。
